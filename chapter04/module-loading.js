// lets load a package (aka a folder)
const sample = require('../sample');

// trying loading biscuit directly
const biscuit = require('../sample/biscuit');

console.info(` sample = ${sample}`);
console.info(`biscuit = ${biscuit}`);
